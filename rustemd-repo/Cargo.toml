[package]
name = "rustemd-repo"
version = "0.1.0"
edition = "2024"
description = "DAO/CRUD layer for rustemd unit files, backed by a swappable repository (plain directory or git)."
license = "MIT"
rust-version = "1.89"

[dependencies]

[dev-dependencies]
tempfile = "3"
