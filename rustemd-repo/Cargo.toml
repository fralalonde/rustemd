[package]
name = "rustemd-repo"
version = "0.1.0"
edition = "2024"
description = "Typed DAO/CRUD layer for rustemd unit definitions backed by directories."
license = "MIT"
rust-version = "1.89"

[dependencies]

[dev-dependencies]
tempfile = "3"
